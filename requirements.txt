# Core Dependencies
fastapi
uvicorn
pydantic
pydantic-settings
email-validator>=2.0.0

# Database
mysql-connector-python
pymongo==4.6.0

# Kafka
kafka-python==2.0.2

# Spark
pyspark==3.5.0

# Machine Learning (streaming sentiment = scikit-learn NB; add transformers/torch if you add HF models back)
scikit-learn
joblib>=1.3.0

# Data Processing
pandas
numpy
pyarrow>=14.0.0

# Utilities
python-dotenv==1.0.0

# Auth (JWT). Do NOT `pip install jose` — that is a different broken package; use python-jose only.
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

